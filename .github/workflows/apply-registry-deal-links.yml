name: Apply registry deal links (disputed pairs)

# 2026-09-07: точечная привязка спорных строк реестра сделок к лидам amoCRM (правило владельца 07.09: самая поздняя не-красная строка таблицы).
# Скрипт scripts/apply-registry-deal-links.js читается с origin/master, данные — из data-ветки
# (data/registry-deal-links.json). Боевой режим требует dry_run=false И confirm_apply=true.

on:
  workflow_dispatch:
    inputs:
      data_branch:
        description: Ветка с data/registry-deal-links.json
        required: true
        default: data/registry-deal-links-20260907
        type: string
      dry_run:
        description: 'Dry-run (только план, ничего не пишется)'
        required: true
        default: true
        type: boolean
      confirm_apply:
        description: 'Подтверждаю запись в прод-БД (нужно вместе с dry_run=false)'
        required: true
        default: false
        type: boolean

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  apply:
    name: Run on production
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: SSH and run
        uses: appleboy/ssh-action@v1.0.3
        env:
          DATA_BRANCH: ${{ inputs.data_branch }}
          DRY_RUN_INPUT: ${{ inputs.dry_run }}
          CONFIRM_APPLY: ${{ inputs.confirm_apply }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script_stop: true
          command_timeout: 25m
          envs: DATA_BRANCH,DRY_RUN_INPUT,CONFIRM_APPLY
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH }}
            git fetch origin "$DATA_BRANCH"
            git fetch origin master
            echo "=== Скрипт из origin/master: $(git rev-parse --short origin/master) ==="
            git show "origin/$DATA_BRANCH:data/registry-deal-links.json" > /tmp/registry-deal-links.json
            git show origin/master:scripts/apply-registry-deal-links.js > /tmp/apply-registry-deal-links.js
            git show origin/master:scripts/enrich-agencies-from-amo.js > /tmp/enrich-agencies-from-amo.js
            docker cp /tmp/registry-deal-links.json st-michael-api:/app/registry-deal-links.json
            docker cp /tmp/apply-registry-deal-links.js st-michael-api:/app/scripts/apply-registry-deal-links.js
            docker cp /tmp/enrich-agencies-from-amo.js st-michael-api:/app/scripts/enrich-agencies-from-amo.js
            MODE=1
            if [ "$DRY_RUN_INPUT" = "false" ] && [ "$CONFIRM_APPLY" = "true" ]; then MODE=0; fi
            if [ "$MODE" = "1" ]; then echo "=== DRY-RUN (без записи) ==="; else echo "=== APPLY (запись в БД) ==="; fi
            docker compose exec -T -e DRY_RUN=$MODE api node /app/scripts/apply-registry-deal-links.js /app/registry-deal-links.json
            echo "=== Готово (DRY_RUN=$MODE) ==="
